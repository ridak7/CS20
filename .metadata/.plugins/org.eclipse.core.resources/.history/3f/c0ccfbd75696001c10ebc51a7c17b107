import java.util.Scanner;
public class RandomNum {

	public static void main(String[] args) 
											{
		
		Scanner Scan = new Scanner (System.in); //Ready for Input
		
		int Numb, Max, Min; //declaration 
		System.out.println("Please enter a two digit number!: "); //Ask for first part
		Numb= Scan.nextInt (); //Record first part
		
		Min = Numb/10;
		Max = (Numb%10);
		
		int rand
		int n = Max - Min + 1;
		int i = rn.nextInt() % n;
		rand=  Min + i;
		
		System.out.println("A random number between the integers you entered is:" + Math.random());
		
		
		

	}

}
